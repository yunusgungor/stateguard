"""Plugin module — Validator extension API.

Modules:
    base:     BaseValidator — abstract base class for all validators
    registry: PluginRegistry — runtime discovery, registration, and management
    examples: Reference validator implementations (JsonSchema, Keyword, Length)
"""
