"""Example validator implementations for the StateGuard plugin system.

This package contains reference implementations of :class:`BaseValidator`
subclasses that serve as examples and defaults:

- :class:`JsonSchemaValidator` — validates JSON schema compliance.
- :class:`KeywordValidator`   — checks for the presence or absence of keywords.
- :class:`LengthValidator`    — validates output length constraints.
"""
